<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novel2script.backend.project.ProjectMapper">

    <resultMap id="ProjectResultMap" type="com.novel2script.backend.project.Project">
        <id property="id" column="id"/>
        <result property="projectUid" column="project_uid"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.novel2script.backend.project.Project" useGeneratedKeys="true" keyProperty="id">
        insert into projects (project_uid, title, status)
        values (#{projectUid}, #{title}, #{status})
    </insert>

    <select id="findById" resultMap="ProjectResultMap">
        select id, project_uid, title, status, created_at, updated_at
        from projects
        where id = #{id}
    </select>

    <select id="findByProjectUid" resultMap="ProjectResultMap">
        select id, project_uid, title, status, created_at, updated_at
        from projects
        where project_uid = #{projectUid}
    </select>

    <update id="updateProjectUid">
        update projects
        set project_uid = #{projectUid},
            updated_at = current_timestamp
        where id = #{id}
    </update>

    <update id="updateStatus">
        update projects
        set status = #{status},
            updated_at = current_timestamp
        where id = #{id}
    </update>
</mapper>
